# AI 简历模板生成提示词

> 复制以下内容，发送给 ChatGPT / DeepSeek / Claude 等 AI 工具，
> 即可生成符合 resume_ai 规范的简历 HTML 模板。

---

## 提示词正文

```
你是一位顶尖的简历设计师和前端工程师。请根据以下规范，为我生成一份精美的简历 HTML 模板。

## 核心要求

### 1. 必须是自包含的单文件 HTML
- 所有 CSS 写在 <style> 标签内，不要引用外部样式表
- 不使用外部字体图标库（可以用 Unicode 符号代替图标）
- 不使用 JavaScript 框架，但可以包含极少量的初始化脚本（可选）

### 2. 可编辑字段标记（非常重要！）
所有需要用户填写的内容，必须用特定属性标记：

**单个字段：**
<span data-editable="字段名">默认占位文字</span>
<div data-editable="字段名">默认占位文字</div>
<p data-editable="字段名">默认占位文字</p>

**列表型字段（如工作经历、教育经历、技能）：**
<div data-editable-group="组名">
  <div data-editable-item>
    <!-- 一组项目中的所有可编辑字段 -->
    <span data-editable="字段名">默认值</span>
  </div>
</div>

**图片型字段（如头像）：**
<img data-editable="字段名" data-editable-type="image" src="默认占位图" />

### 3. 需要包含的字段

**基本信息（必选）：**
- name（姓名）
- title（职位头衔）
- email（邮箱）
- phone（电话）
- location（城市/地点）
- website（个人网站/链接，可选）
- avatar（头像照片，可选，用 data-editable-type="image"）

**个人简介（必选）：**
- summary（个人简介，2-3句话）

**工作经历（推荐，可编辑组）：**
- work-experience（组）
  - company（公司名称）
  - position（职位）
  - duration（时间段）
  - description（工作描述，允许换行）

**教育经历（推荐，可编辑组）：**
- education（组）
  - school（学校名称）
  - degree（学位/专业）
  - duration（时间段）

**技能（推荐）：**
- skills（技能列表，可用逗号分隔或列表形式）

### 4. 设计风格要求
- 专业、现代、简洁
- 适合打印（A4纸），宽度控制在 800px 以内
- 使用 2-3 种协调的颜色（主色+辅色+中性色）
- 字体大小层次分明（姓名最大，标题次之，正文最小）
- 留白合理，信息密度适中
- 必须包含 @media print 样式以优化打印效果
- 简历整体控制在1页A4纸以内（210mm × 297mm）

### 5. 排版布局建议
- 可采用单栏或双栏布局（左栏窄放技能/联系方式，右栏宽放经历）
- 头部突出显示姓名和头衔
- 每个区块有清晰的分隔线或间距

### 6. 默认占位文字
- 用中文填写有意义的示例内容（如"张三"、"高级前端工程师"）
- 不要留空，让模板看起来是完整的效果

### 7. 输出格式
- 直接输出完整的 HTML 代码
- 用 ```html 代码块包裹
- 不要输出多余的解释文字

请开始生成。
```

---

## 使用方式

1. 复制上面的提示词
2. 发送给 ChatGPT / DeepSeek / Claude
3. 将生成的 HTML 保存为文件
4. 上传到 resume_ai 平台，或直接复制到 `templates/` 目录

## 进阶用法

可以在提示词末尾追加风格要求，例如：

- "请设计成极简黑白风格"
- "请用蓝色系配色，适合技术岗位"
- "请使用双栏布局，左侧窄栏放联系方式"
- "请设计成适合设计师的创意风格，使用大胆的配色"
